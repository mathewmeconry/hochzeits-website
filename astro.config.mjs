// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // [PLACEHOLDER] Set to your GitHub Pages URL or custom domain
  // e.g. 'https://username.github.io' for a root page, or 'https://www.example.com'
  site: 'https://username.github.io/hochzeits-website', // [PLACEHOLDER]

  // [PLACEHOLDER] If deploying to a GitHub Pages *project* page (not a custom domain or
  // root user/org page), uncomment the line below so asset URLs are correct:
  // base: '/hochzeits-website', // [PLACEHOLDER]

  output: 'static',
});
